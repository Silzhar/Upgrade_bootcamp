// Objetos .

// for - in itera sobre las propiedades de un objeto .
for ( x in y ){ };  


var btn = document.getElementById("mi-boton");
// (btn) Es para comprobar si es true, que no esté vacio o sea nulo. Sintaxis mas utilizada.
if(btn){
    btn.addEventListener(...)
}

// Pasar estilo en html .
<p style = ""> </p>

// Buscar elemento <select> .
style = "border: 4px solid blue; padding: 20px;"

// Para el comportamiento predefinido . Ejempo: para frenar envío de formulario (edad inferior..etc)
 event.preventDefault()
